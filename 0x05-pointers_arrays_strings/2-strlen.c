#include "main.h"
#include <stdio.h>

/**
 * _strlen - find the length of string
 * @c: The string
 * Return: The length of string c
 */
int _strlen(char *c)
{
	int len;

	for (len = 0; c[len] != '\0';len++)
	{
	}
	return (len);
}
